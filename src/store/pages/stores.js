//import { stores as Configure } from './configure'
//import { getConfig } from 'common/api'
const state = {
  config: {},
}

const getters = {
  // subjects: state => state.config.subjects || [],
  // courseTypes: state => state.config.courseTypes || [],
  // classTypes: state => state.config.classTypes || [],
  // subjectIdMap: state => state.config.subjectIdMap || {},
  // courseTypeIdMap: state => state.config.courseTypeIdMap || {},
  // classTypeIdMap: state => state.config.classTypeIdMap || {},
}

const mutations = {
  // UPDATE_CONFIG(state, payload) {
  //   state.config = payload
  // } 
}

const actions = {
  // async AC_GetConfig({ commit }, params) {
  //   let config = await getConfig()
  //   console.log(config)
  //   commit('UPDATE_CONFIG', config[0])
  // }
}

export const stores = {
  state,
  // getters,
  // mutations,
  // actions,
  // modules: {
  //   Configure
  // }
}
